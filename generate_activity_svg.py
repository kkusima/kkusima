import svgwrite\n\n# Initialize the SVG drawing\ndwg = svgwrite.Drawing('days-2026.svg', profile='tiny')\n\n# Settings for the activity representation\nmax_days = 365\nactivity_data = [0] * max_days  # This will hold activity count for each day\n\n# Simulate some activity data for demonstration (You can replace this with actual data)\nimport random\nfor day in range(max_days):\n    activity_data[day] = random.randint(0, 10)  # Random activity count between 0 and 10\n\n# Draw the activity bars\nfor day in range(max_days):\n    x = day * 2  # Space out days\n    height = activity_data[day] * 10  # Scale height according to activity\n    dwg.add(dwg.rect(insert=(x, 100-height), size=(1.5, height), fill='blue'))\n\n# Save the SVG file\ndwg.save()